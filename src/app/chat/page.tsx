import Placeholder from "@/components/Placeholder";

export default function ChatPage() {
  return (
    <Placeholder
      title="Chat"
      note="Each board will have its own chat, coming in a later section."
    />
  );
}
