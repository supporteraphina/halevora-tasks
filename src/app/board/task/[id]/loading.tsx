import TaskPanelSkeleton from "../TaskPanelSkeleton";

// Instant skeleton for the full-page (deep-link) task route while its detail loads.
export default function Loading() {
  return <TaskPanelSkeleton />;
}
